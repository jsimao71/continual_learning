# Diagnostic only

This directory was produced before the budget-seed audit. Its T2 runs used
different initialization and data-order seeds from T1, so they are not a
matched training-budget comparison and must not be used for scientific claims.

The corrected matched T2 results are in `learned_v2_matched/`.
